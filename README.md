
Need meteor?
```
curl https://install.meteor.com/ | sh
```

```
$ git clone https://github.com/uport-project/uport-meteor.git
$ cd uport-meteor
$ meteor npm install
$ rm -rf node_modules/crypto
$ meteor
```
